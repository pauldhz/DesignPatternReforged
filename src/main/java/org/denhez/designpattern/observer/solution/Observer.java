package org.denhez.designpattern.observer.solution;

public interface Observer {

    // Equivalent to Consumer<Double>. Just keep it for understanding what Observer is used with.
    void data (double value);
}
